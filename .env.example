# ====================================================================
# HONEYPOT API - ENVIRONMENT CONFIGURATION
# ====================================================================
# 
# This file contains all environment variables needed for the honeypot API.
# Copy this file to '.env' and fill in your actual values.
#
# SECURITY WARNING: Never commit .env to git! It's already in .gitignore.
# ====================================================================

# --------------------------------------------------------------------
# 1. HONEYPOT API KEY (Required)
# --------------------------------------------------------------------
# This is YOUR secret API key that clients must provide in the 
# x-api-key header to access your honeypot endpoint.
#
# Generate a secure key using one of these methods:
#   - Python: python -c "import secrets; print(secrets.token_urlsafe(32))"
#   - OpenSSL: openssl rand -hex 32
#   - Online: https://randomkeygen.com/
#
# Example: HONEYPOT_API_KEY=8xK9mN2pQ5rT7vW0zA3bC6dE9fG2hJ5k
HONEYPOT_API_KEY=your_honeypot_api_key_here


# --------------------------------------------------------------------
# 2. GROQ API KEY (Required)
# --------------------------------------------------------------------
# Get your FREE Groq API key from: https://console.groq.com/keys
#
# Steps to get your Groq API key:
#   1. Go to https://console.groq.com/
#   2. Sign up or log in (free account)
#   3. Navigate to "API Keys" section
#   4. Click "Create API Key"
#   5. Copy the key (starts with "gsk_")
#
# Free Tier Limits:
#   - 14,400 requests per day
#   - 30 requests per minute
#   - More than enough for the hackathon!
#
# Example: GROQ_API_KEY=gsk_abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
GROQ_API_KEY=gsk_your_groq_api_key_here


# --------------------------------------------------------------------
# 3. SERVER CONFIGURATION (Optional)
# --------------------------------------------------------------------
# Port for local development (Railway/Render will override this)
PORT=8000

# Environment (development, staging, production)
ENVIRONMENT=production

# Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO


# --------------------------------------------------------------------
# 4. GUVI CALLBACK URL (Auto-configured, don't change)
# --------------------------------------------------------------------
# This is the GUVI endpoint where final results are sent.
# DO NOT MODIFY unless instructed by GUVI.
GUVI_CALLBACK_URL=https://hackathon.guvi.in/api/updateHoneyPotFinalResult


# --------------------------------------------------------------------
# 5. ADVANCED SETTINGS (Optional - Use defaults)
# --------------------------------------------------------------------
# Scam detection threshold (0-100, default: 55)
# Lower = more sensitive, Higher = more specific
SCAM_DETECTION_THRESHOLD=55

# Maximum conversation turns before auto-end (default: 15)
MAX_CONVERSATION_TURNS=15

# Minimum turns before sending final report (default: 5)
MIN_TURNS_BEFORE_REPORT=5

# Groq model to use (default: llama-3.3-70b-versatile)
# Options: llama-3.3-70b-versatile, mixtral-8x7b-32768
GROQ_MODEL=llama-3.3-70b-versatile

# Maximum tokens for AI response (default: 100)
# Lower = faster but shorter responses
AI_MAX_TOKENS=100

# AI temperature (0.0-1.0, default: 0.8)
# Higher = more creative, Lower = more focused
AI_TEMPERATURE=0.8


# ====================================================================
# QUICK SETUP CHECKLIST:
# ====================================================================
# 
# [ ] 1. Copy this file: cp .env.example .env
# [ ] 2. Generate HONEYPOT_API_KEY (see instructions above)
# [ ] 3. Get GROQ_API_KEY from https://console.groq.com/keys
# [ ] 4. Replace the placeholder values above
# [ ] 5. Save this file
# [ ] 6. Start the server: python main.py
# [ ] 7. Test locally: python test_api.py
# [ ] 8. Deploy to Railway/Render/Fly.io
# [ ] 9. Set the same environment variables in your deployment platform
# [ ] 10. Submit to GUVI!
#
# ====================================================================
# IMPORTANT NOTES:
# ====================================================================
#
# 1. SECURITY:
#    - Never share your .env file
#    - Never commit .env to git
#    - Use different keys for dev/staging/production
#
# 2. DEPLOYMENT:
#    - Railway: Set variables in dashboard → Variables tab
#    - Render: Set in dashboard → Environment tab
#    - Fly.io: Use `fly secrets set KEY=value`
#
# 3. TESTING:
#    - Test Groq connection: python -c "from groq import Groq; print('OK')"
#    - Test API: curl http://localhost:8000/health
#
# 4. TROUBLESHOOTING:
#    - "Invalid API key" → Check HONEYPOT_API_KEY matches x-api-key header
#    - "Groq error" → Verify GROQ_API_KEY is correct and active
#    - "Connection refused" → Make sure server is running
#
# ====================================================================
# SUPPORT:
# ====================================================================
# 
# Groq Issues: https://console.groq.com/docs
# Railway Help: https://docs.railway.app/
# Render Help: https://render.com/docs
#
# ====================================================================